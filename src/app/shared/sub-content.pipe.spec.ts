import { SubContentPipe } from './sub-content.pipe';

describe('SubContentPipe', () => {
  it('create an instance', () => {
    const pipe = new SubContentPipe();
    expect(pipe).toBeTruthy();
  });
});
